package server

import "net"

// Serve is a stub function for the server package
// The actual implementation is excluded via build constraints
func Serve(ln net.Listener) error {
	// Stub implementation - actual server is disabled
	return nil
}
